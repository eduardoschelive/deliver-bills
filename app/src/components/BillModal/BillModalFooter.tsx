import { ModalFooter } from '@nextui-org/react'
import { PropsWithChildren } from 'react'

export const BillModalFooter = ({ children }: PropsWithChildren) => {
  return <ModalFooter>{children}</ModalFooter>
}
